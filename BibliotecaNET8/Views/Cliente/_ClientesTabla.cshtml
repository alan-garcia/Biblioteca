@model PagedResult<ClienteVM>

@if (!Model.Items.Any())
{
    <div class="libro-alert libro-alert-info" role="alert">
        <span class="font-medium">@Localizer["ClientesSearchNotFound"]</span>
    </div>
}
else
{
    <!-- Enlaces de navegación -->
    @await Html.PartialAsync("_Pagination", Model)

    <table class="text-gray-500 w-full text-left text-sm">
        <thead class="bg-gray-100 text-gray-700 text-xs">
            <tr>
                <th scope="col" class="p-4">
                    <div class="flex items-center">
                        <input name="allIdsCliente" type="checkbox" class="libro-checkbox-input">
                        <label for="checkbox-all-search" class="sr-only">checkbox</label>
                    </div>
                </th>
                <th scope="col" class="px-6 py-3">@Html.DisplayNameFor(model => model.Items.First().Nombre)</th>
                <th scope="col" class="px-6 py-3">@Html.DisplayNameFor(model => model.Items.First().Apellido)</th>
                <th scope="col" class="px-6 py-3">@Html.DisplayNameFor(model => model.Items.First().Email)</th>
                <th scope="col" class="px-6 py-3">@Html.DisplayNameFor(model => model.Items.First().Telefono)</th>
                <th scope="col" class="px-6 py-3">@Localizer["AccionesTitle"]</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var cliente in Model.Items)
            {
                <tr class="bg-white border-b">
                    <td class="w-4 p-4">
                        <div class="flex items-center">
                            <input name="idsCliente" type="checkbox" value="@cliente.Id" class="libro-checkbox-input">
                            <label for="checkbox-table-search-1" class="sr-only">checkbox</label>
                        </div>
                    </td>
                    <td scope="row" class="libro-table-row-content">@cliente.Nombre</td>
                    <td scope="row" class="libro-table-row-content">@cliente.Apellido</td>
                    <td scope="row" class="libro-table-row-content">@cliente.Email</td>
                    <td scope="row" class="libro-table-row-content">@cliente.Telefono</td>
                    <td class="px-6 py-4">
                        <a asp-route="clienteVer" asp-route-id="@cliente.Id" class="libro-btn libro-btn-info">@Localizer["VerButton"]</a>
                        <a asp-route="clienteEdit" asp-route-id="@cliente.Id" class="libro-btn libro-btn-edit">@Localizer["ModificarButton"]</a>
                        <a asp-route="clienteDelete" asp-route-id="@cliente.Id" onclick="Eliminar(this.href); return false;" class="libro-btn libro-btn-delete cursor-pointer">@Localizer["EliminarButton"]</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <div class="my-4">
        <a asp-route="clienteDeleteMultiple" onclick="EliminarSeleccionados(this.href); return false;" class="libro-btn libro-btn-delete">@Localizer["EliminarSelectedButton"]</a>
    </div>
}
