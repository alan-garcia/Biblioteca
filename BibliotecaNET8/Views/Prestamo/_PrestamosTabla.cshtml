@model PagedResult<PrestamoListVM>

@if (!Model.Items.Any())
{
    <div class="bbtc-alert bbtc-alert-info" role="alert">
        <span class="font-medium">@Localizer["PrestamosSearchNotFound"]</span>
    </div>
}
else
{
    @await Html.PartialAsync("_Pagination", Model)

    <table class="w-full text-left text-sm text-gray-500">
        <thead class="bg-gray-200 text-xs text-gray-700">
            <tr>
                <th scope="col" class="p-4">
                    <div class="flex items-center">
                        <input name="allIdsLibro" type="checkbox" class="bbtccheckbox-input">
                        <label for="checkbox-all-search" class="sr-only">checkbox</label>
                    </div>
                </th>
                <th scope="col" class="px-6 py-3">@Html.DisplayNameFor(model => model.Items.First().FechaPrestamo)</th>
                <th scope="col" class="px-6 py-3">@Html.DisplayNameFor(model => model.Items.First().FechaDevolucion)</th>
                <th scope="col" class="px-6 py-3">@Html.DisplayNameFor(model => model.Items.First().ClienteId)</th>
                <th scope="col" class="px-6 py-3">@Html.DisplayNameFor(model => model.Items.First().LibroId)</th>
                <th scope="col" class="px-6 py-3">@Localizer["AccionesTitle"]</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var prestamo in Model.Items)
            {
                <tr class="border-b bg-white">
                    <td class="w-4 p-4">
                        <div class="flex items-center">
                            <input name="idsPrestamo" type="checkbox" value="@prestamo.Id" class="bbtccheckbox-input">
                            <label for="checkbox-table-search-1" class="sr-only">checkbox</label>
                        </div>
                    </td>
                    <td scope="row" class="bbtctable-row-content">@prestamo.FechaPrestamo.ToShortDateString()</td>
                    <td scope="row" class="bbtctable-row-content">@prestamo.FechaDevolucion.Value.ToShortDateString()</td>
                    <td scope="row" class="bbtctable-row-content">@($"{prestamo.Cliente.Nombre} {prestamo.Cliente.Apellido}")</td>
                    <td scope="row" class="bbtctable-row-content">@prestamo.Libro.Titulo</td>
                    <td class="px-6 py-4">
                        <a asp-route="prestamoVer" asp-route-id="@prestamo.Id" class="bbtc-btn bbtc-btn-alternative mr-2">
                            <icon href="view-icon" class="size-6"></icon> @Localizer["VerButton"]
                        </a>
                        <a asp-route="prestamoEdit" asp-route-id="@prestamo.Id" class="bbtc-btn bbtc-btn-info mr-2">
                            <icon href="edit-icon" class="size-6"></icon> @Localizer["ModificarButton"]
                        </a>
                        <a asp-route="prestamoDelete" asp-route-id="@prestamo.Id" onclick="Eliminar(this.href); return false;"
                            class="bbtc-btn bbtc-btn-delete-outline mr-2">
                            <icon href="delete-icon" class="size-6"></icon> @Localizer["EliminarButton"]
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <div class="my-4">
        <a asp-route="prestamoDeleteMultiple" onclick="EliminarSeleccionados(this.href); return false;"
           class="bbtc-btn bbtc-btn-delete-outline ml-2">
            <icon href="delete-icon" class="size-6"></icon> @Localizer["EliminarSelectedButton"]
        </a>
    </div>
}
