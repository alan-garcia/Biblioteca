@model PagedResult<CategoriaVM>

@if (!Model.Items.Any())
{
    <div class="libro-alert libro-alert-info" role="alert">
        <span class="font-medium">@Localizer["CategoriasSearchNotFound"]</span>
    </div>
}
else
{
    <!-- Enlaces de navegación -->
    @await Html.PartialAsync("_Pagination", Model)

    <table class="text-gray-500 w-full text-left text-sm">
        <thead class="bg-gray-100 text-gray-700 text-xs">
            <tr>
                <th scope="col" class="p-4">
                    <div class="flex items-center">
                        <input name="allIdsCategoria" type="checkbox" class="libro-checkbox-input">
                        <label for="checkbox-all-search" class="sr-only">checkbox</label>
                    </div>
                </th>
                <th scope="col" class="px-6 py-3">@Html.DisplayNameFor(model => model.Items.First().Nombre)</th>
                <th scope="col" class="px-6 py-3">@Localizer["AccionesTitle"]</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var categoria in Model.Items)
            {
                <tr class="bg-white border-b">
                    <td class="w-4 p-4">
                        <div class="flex items-center">
                            <input name="idsCategoria" type="checkbox" value="@categoria.Id" class="libro-checkbox-input">
                            <label for="checkbox-table-search-1" class="sr-only">checkbox</label>
                        </div>
                    </td>
                    <td scope="row" class="libro-table-row-content">@categoria.Nombre</td>
                    <td class="px-6 py-4">
                        <a asp-route="categoriaVer" asp-route-id="@categoria.Id" class="libro-btn libro-btn-info">@Localizer["VerButton"]</a>
                        <a asp-route="categoriaEdit" asp-route-id="@categoria.Id" class="libro-btn libro-btn-edit">@Localizer["ModificarButton"]</a>
                        <a asp-route="categoriaDelete" asp-route-id="@categoria.Id" onclick="Eliminar(this.href); return false;" class="libro-btn libro-btn-delete cursor-pointer">@Localizer["EliminarButton"]</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <div class="my-4">
        <a asp-route="categoriaDeleteMultiple" onclick="EliminarSeleccionados(this.href); return false;" class="libro-btn libro-btn-delete">@Localizer["EliminarSelectedButton"]</a>
    </div>
}
