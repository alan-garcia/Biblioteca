@model PagedResult<CategoriaVM>

<h1 class="bbtc-title">@Localizer["ListaCategoriasTitle"]</h1>

<div class="mb-6 mt-8">
    <a asp-route="categoriaCreate" class="bbtc-btn bbtc-btn-info">
        <icon href="add-icon" class="size-6"></icon> @Localizer["CrearCategoriaButton"]
    </a>
</div>

@if (TempData["CategoriasMensaje"] != null)
{
    <div class="bbtc-alert bbtc-alert-success" role="alert">
        <span class="font-medium">@TempData["CategoriasMensaje"]</span>
    </div>
}

@if (ViewBag.ListaCategorias != null)
{
    <div class="bbtc-alert bbtc-alert-info" role="alert">
        <span class="font-medium">@ViewBag.ListaCategorias</span>
    </div>
}
else
{
    <div class="relative overflow-x-auto shadow-md sm:rounded-lg">
        <div class="bg-white pb-4">
            <label for="filterSearch" class="sr-only">Search</label>
            <div class="relative mt-1">
                <div class="pointer-events-none absolute inset-y-0 start-0 flex items-center ps-3">
                    <icon href="search-icon" class="size-6"></icon>
                </div>
                <input type="text" name="filterSearch" onkeyup="Search('/Categoria/Search');" class="bbtc-search-filter-input" placeholder='@(Localizer["CategoriaSearchPlaceholder"])' autofocus>
            </div>
        </div>

        <div id="dataBody">
            @await Html.PartialAsync("_CategoriasTabla", Model)
        </div>
    </div>
}
